<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 设置包级别的日志级别，减少控制台输出 -->
    <logger name="com.hgl.hglrpc" level="INFO">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 提高etcd客户端的日志级别，减少控制台输出 -->
    <logger name="io.etcd.jetcd" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 提高netty日志级别，减少grpc通信日志 -->
    <logger name="io.grpc.netty" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 根日志级别设置为INFO，输出INFO及以上级别信息 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
